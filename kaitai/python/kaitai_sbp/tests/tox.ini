[tox]
requires = tox-run-command
skipsdist = True
envlist = py

[testenv]
deps =
  pytest
  kaitaistruct==0.10.0
  construct
  python-rapidjson
changedir = ../../../..
commands = pytest -s {posargs}
setenv   =
    PYTHONPATH = kaitai/python:python:.
