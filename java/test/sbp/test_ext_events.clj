(ns sbp.core-test
  (:require
   [clojure.test :refer :all])
  (:import
   [swiftnav.sbp SBP]
   [swiftnav.sbp.ext_events
    MsgExtEvent]))

(deftest a-test
  (testing "FIXME, I fail."
    (is (= 0 1))))
