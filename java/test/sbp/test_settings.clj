(ns sbp.test-settings
  (:require
   [clojure.test :refer :all])
  (:import
   [swiftnav.sbp SBP]
   [swiftnav.sbp.settings
    MsgSettings
    MsgSettingsReadByIndex
    MsgSettingsSaven]))

(deftest a-test
  (testing "FIXME, I fail."
    (is (= 0 1))))
