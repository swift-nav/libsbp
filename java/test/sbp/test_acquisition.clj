(ns sbp.test-acquisition
  (:require
   [clojure.test :refer :all])
  (:import
   [swiftnav.sbp
    SBP]
   [swiftnav.sbp.acquisition
    MsgAcqResult]))

(deftest a-test
  (testing "FIXME, I fail."
    (is (= 0 1))))
