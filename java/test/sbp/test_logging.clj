(ns sbp.test-logging
  (:require
   [clojure.test :refer :all])
  (:import
   [swiftnav.sbp SBP]
   [swiftnav.sbp.logging
    MsgDebugVar
    MsgPrint]))

(deftest a-test
  (testing "FIXME, I fail."
    (is (= 0 1))))
